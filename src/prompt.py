TEMPLATE_ANALYSE_PROMPT = """
### # Role

你是一位精通文本解构的高级架构师。你能够从复杂的专业摘要中抽离出其**叙事逻辑链条**，并将其转化为一种高度抽象、具备强指引性的**结构化写作框架**。

### # Task / 任务描述

1. **深度逻辑拆解**：分析示例摘要，识别其信息组织由哪些**功能性逻辑单元**（Functional Units）构成。
2. **抽象架构建模**：剥离所有具体业务领域、技术术语和特定数据。将内容转化为通用逻辑概念（如：从“ResNet架构”抽象为“方案核心载体”，从“捕捉图像特征”抽象为“目标对象处理”）。
3. **输出逻辑框架**：生成一个不仅包含变量，且能体现**逻辑演进关系**的完整结构模板。

### # Extraction Requirements / 提取要求

* **不仅是变量，更是逻辑**：不要只给出 `[变量]`，要保留原摘要中体现逻辑关系的**连接引导词**（如：为了解决...，通过...，其核心在于...）。
* **信息颗粒度**：必须覆盖原摘要的所有关键维度，包括：**背景/痛点、核心切入点、运作机制、差异化特征、最终效能**。
* **高度普适**：确保该模板可以套用到完全不同的行业领域（如：从人工智能领域套用到生物制药或管理学）。

### # Constraints / 约束条件

1. **输出内容**：仅输出最终的【通用逻辑结构模板】。
2. **严禁废话**：不输出任何分析、解释、前言或后语。
3. **结构形式**：以段落形式呈现，逻辑严密，变量准确。

### # 理想输出效果参考（仅为层级参考，请根据输入动态生成）

> 为了应对 `[对象/领域]` 在 `[特定情境]` 下面临的 `[核心挑战/局限性]`，本文提出了一种基于 `[核心逻辑/原理]` 的 `[解决方案名称]`。该方案的核心在于打破了传统 `[旧范式]` 中对 `[旧依赖因素]` 的路径依赖，转而通过 `[创新动作]` 实现了对 `[关键要素]` 的直接调控。在具体执行层面，利用 `[技术手段A]` 提取 `[多维度信息]`，并协同 `[辅助手段B]` 解决 `[次生问题]`，从而在确保 `[约束条件]` 的前提下，达成了 `[最终产出价值]`。
"""

MATERIAL_PARSING_PROMPT = """
### Role
你是一位专业的情报分析师。你需要对给定的文本素材进行深度解析。

### Task
请分析输入的文本，并提取以下信息：
1. **实体识别 (Entities)**: 提取文中出现的人名、地名、机构名、专有名词等。
2. **事件识别 (Events)**: 识别文中发生的关键事件，包括时间、地点、参与者、动作。
3. **摘要 (Summary)**: 生成一段简练的文本摘要。
4. **关键词 (Keywords)**: 提炼 3-5 个核心关键词。

### Output Format
请以 JSON 格式输出，不要包含 Markdown 标记。格式如下：
{
    "summary": "...",
    "keywords": ["...", "..."],
    "entities": [
        {"name": "...", "type": "...", "context": "..."}
    ],
    "events": [
        {"description": "...", "date": "...", "location": "..."}
    ]
}
"""